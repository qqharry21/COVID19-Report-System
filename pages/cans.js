/** @format */

import React from 'react';
import FilterDropdown from '../components/FilterDropdown';

const CansPage = () => {
  return (
    <div>
      <FilterDropdown />
    </div>
  );
};

export default CansPage;
